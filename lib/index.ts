import {
  cliOf,
  useImport,
  usePath,
  useCustom,
  useModuleName
} from "./decoration";

import { configure } from "./config";

export { cliOf, configure, useImport, usePath, useCustom, useModuleName };
